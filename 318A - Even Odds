#include<stdio.h>
int main()
{
   long long int n,k,a,b;
 
   scanf("%lld%lld",&n,&k);
 
  if(n%2==0)
    a=n/2;
  else
    a=n/2+1;
  if(k<=a)
    b=(k*2)-1;
  else
    b=(k-a)*2;
 
   printf("%lld\n",b);
 
   return 0;
}
